# 股票预测异构图神经网络模型架构

## 项目概述

本项目设计了一个基于异构图神经网络的股票预测模型，旨在通过15天的窗口数据预测未来5日和30日的股价波动率和股价波动方向。

## 数据结构

### 异构图数据结构
- **节点类型**:
  - `stock`: 股票节点，每个节点具有34维特征
  - `comment`: 评论节点，每个节点具有768维特征

- **边类型**:
  - `stock -> connect -> comment`: 股票与评论的连接关系
  - `comment -> connect_rev -> stock`: 评论到股票的反向连接关系

### 时间序列处理
- 模型接收连续15天的异构图数据作为输入
- 每天的数据包含股票特征和评论特征以及它们之间的连接关系

## 模型架构

### 1. 异构图神经网络层 (HeteroGNN)

#### HeteroGNNBlock
- 使用PyTorch Geometric的`HeteroConv`和`SAGEConv`实现
- 处理不同节点类型之间的信息传播
- 包含多层异构卷积，逐步提取高阶特征

#### 节点嵌入层
- `stock_embedding`: 将34维股票特征映射到隐藏维度
- `comment_embedding`: 将768维评论特征映射到隐藏维度

### 2. 时间序列处理层

#### LSTM层
- 处理15天序列的节点聚合特征
- 捕捉时间维度上的依赖关系
- 输出序列的最终状态作为预测的输入

### 3. 预测头

#### 波动率预测头
- `volatility_5d_head`: 预测5日股价波动率
- `volatility_30d_head`: 预测30日股价波动率
- 使用Sigmoid激活函数，输出范围[0,1]

#### 方向预测头
- `direction_5d_head`: 预测5日股价波动方向
- `direction_30d_head`: 预测30日股价波动方向
- 使用Tanh激活函数，输出范围[-1,1]

## 模型特点

### 优势
1. **异构信息融合**: 有效整合股票特征和评论信息
2. **时空建模**: 同时处理图结构和时间序列信息
3. **多任务学习**: 同时预测波动率和方向，5日和30日
4. **可解释性**: 异构图结构便于分析不同信息源的影响

### 技术细节
- 使用GraphSAGE卷积处理异构图
- LSTM捕获时间依赖性
- 批归一化和Dropout防止过拟合
- 梯度裁剪稳定训练过程

## 训练策略

### 损失函数
- 波动率预测: MSE损失
- 方向预测: MSE损失 (因方向为连续值)

### 优化器
- AdamW优化器
- 学习率调度器: ReduceLROnPlateau

## 实际应用考虑

### 数据预处理
1. 股票特征标准化
2. 评论特征降维或聚合
3. 图结构稀疏化处理

### 模型优化
1. 图注意力机制增强重要连接
2. 多头注意力机制
3. 残差连接增强梯度流动

### 评估指标
- MAE (平均绝对误差) 
- RMSE (均方根误差)
- 方向准确性 (预测方向与实际方向的一致性)

## 代码结构

```
/workspace/
├── stock_prediction_model.py     # 主模型定义
├── data_processor.py            # 数据处理工具
├── train_stock_prediction.py    # 训练脚本
└── model_architecture.md        # 架构文档
```

## 模型扩展性

该架构支持以下扩展：
- 添加更多节点类型（如新闻、财务数据）
- 增加预测时间窗口
- 集成更多金融指标
- 使用更复杂的图神经网络变体

## 总结

本模型通过异构图神经网络有效整合股票特征和评论信息，结合LSTM处理时间序列，能够同时预测多个时间窗口的股价波动率和方向，为股票市场预测提供了一个强大的深度学习解决方案。